/* *********************
Algorithm sortRB(L)
curr: = L.isFirst()
while curr.element = B do
    currElem = curr.element()
    curr = curr.next()
    while curr != L.last()
        if curr = R
        swapElements(currElem, curr.element())
* *********************/
